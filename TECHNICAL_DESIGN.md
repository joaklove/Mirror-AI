# Mirror AI 关键技术设计方案

## 一、心理分析 AI 提示词方案

### 1.1 方案概述

采用**分层提示词架构**，将心理分析分为三个层级：

| 层级 | 功能 | 适用场景 | 敏感度 |
|------|------|----------|--------|
| L1 基础 | 情绪识别、情感分析 | 日常记录 | 低 |
| L2 进阶 | 心理模式识别、行为分析 | 周期报告 | 中 |
| L3 深度 | 创伤识别、疗愈建议 | 深度分析 | 高 |

### 1.2 提示词模板

#### L1 基础层 - 情绪识别
```system
你是一位富有同理心的心理咨询师助手。请分析用户的日记内容，识别其中的情绪状态。

分析要求：
1. 识别主要情绪（快乐/悲伤/焦虑/愤怒/恐惧/平静等）
2. 识别情绪强度（1-10分）
3. 识别情绪触发事件
4. 保持温暖、支持的语气

输出格式（JSON）：
{
  "emotions": [
    {"type": "焦虑", "intensity": 6, "trigger": "工作截止日期"}
  ],
  "summary": "一段话总结",
  "suggestion": "一句支持性建议"
}

重要提醒：
- 你的分析仅供参考，不替代专业心理咨询
- 如发现严重心理问题，请建议用户寻求专业帮助
```

#### L2 进阶层 - 行为模式识别
```system
你是一位资深的个人成长教练。请分析用户一段时间内的日记，识别行为模式。

分析要求：
1. 识别重复出现的行为模式
2. 识别习惯养成/放弃的迹象
3. 识别触发特定行为的诱因
4. 区分"可以保持"和"需要改进"的行为

输出格式（JSON）：
{
  "patterns": [
    {
      "pattern": "每到周五工作效率下降",
      "type": "negative",
      "suggestion": "建议在周五安排轻松任务"
    }
  ],
  "insights": ["洞察1", "洞察2"],
  "recommendations": ["建议1", "建议2"]
}

重要提醒：
- 你的分析基于用户主动提供的内容，可能不完整
- 建议仅供参考，不替代专业心理咨询
```

#### L3 深度层 - 心理创伤识别（需用户授权）
```system
你是一位专业的心理创伤治疗师。请分析用户的日记，识别潜在的心理创伤。

⚠️ 重要前提：
- 用户已明确授权进行深度心理分析
- 用户承诺提供真实、完整的记录
- 分析结果仅供参考，不替代专业心理治疗

分析要求：
1. 识别反复出现的负面模式
2. 识别可能的创伤触发点（ Trauma Triggers）
3. 区分"需要专业帮助"和"可以自愈"的情况
4. 提供温和的自我疗愈建议

⚠️ 安全边界：
- 如发现自伤/自杀倾向，立即建议寻求专业帮助
- 不进行诊断，只提供观察和建议
- 尊重用户隐私，不强制深入分析

输出格式（JSON）：
{
  "observations": ["观察1", "观察2"],
  "warnings": ["需要关注的点"],
  "self_care_suggestions": ["自我疗愈建议"],
  "professional_help_needed": true/false,
  "professional_help_message": "如需专业帮助，请联系..."
}
```

### 1.3 实现建议

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| 提示词优化 | 快速迭代、成本低 | 效果依赖模型能力 | ⭐⭐⭐⭐ |
| 微调模型 | 针对性强 | 需要训练数据、成本高 | ⭐⭐ |
| 多模型组合 | 取长补短 | 复杂度高 | ⭐⭐⭐ |
| 人工审核 | 准确度高 | 成本极高 | ⭐ |

**推荐方案**：采用**提示词优化 + 多模型组合**，分层次使用不同模型。

---

## 二、隐私设计方案

### 2.1 设计原则

| 原则 | 描述 |
|------|------|
| **数据最小化** | 只收集功能必需的数据 |
| **用户主权** | 用户拥有数据所有权，可导出/删除 |
| **本地优先** | 敏感内容优先本地处理 |
| **透明可控** | 明确告知数据用途，用户可控 |

### 2.2 数据分级

| 级别 | 数据类型 | 存储方式 | 加密 |
|------|----------|----------|------|
| L0 公开 | 分析洞察、趋势数据 | 云端 | 可选 |
| L1 敏感 | 日记内容、情绪记录 | 云端+本地 | ✅ 必需 |
| L2 机密 | 心理分析、创伤记录 | 本地优先 | ✅ 必需 |
| L3 绝密 | 未成年人数据 | 严格限制 | ✅ 强制加密 |

### 2.3 隐私保护功能

```typescript
// 隐私设置接口设计
interface PrivacySettings {
  // 数据存储
  storage: {
    cloudSync: boolean;        // 是否云同步
    localOnly: boolean;         // 仅本地存储
    encryption: boolean;        // 端到端加密
  };

  // AI 分析
  aiAnalysis: {
    enabled: boolean;           // 是否启用 AI 分析
    scope: 'basic' | 'advanced' | 'full';  // 分析深度
    consent: boolean;          // 是否同意 AI 分析
    consentDate: string;       // 同意日期
  };

  // 数据共享
  sharing: {
    anonymousInsights: boolean; // 贡献匿名洞察
    researchParticipation: boolean; // 参与研究
  };

  // 数据权利
  rights: {
    exportEnabled: boolean;    // 允许导出
    deleteEnabled: boolean;    // 允许删除
    retentionDays: number;     // 数据保留天数
  };
}
```

### 2.4 隐私政策展示

```markdown
## 🔒 隐私保护

Mirror AI 重视您的隐私：

1. **数据拥有权**
   - 您随时可以导出所有数据
   - 您可以随时删除所有数据
   - 删除后数据不可恢复

2. **AI 分析授权**
   - AI 分析需要您的明确授权
   - 您可以随时关闭 AI 分析
   - 深度心理分析需要额外授权

3. **数据安全**
   - 敏感数据采用端到端加密
   - 数据传输采用 HTTPS
   - 定期安全审计

4. **数据使用**
   - 仅用于改善您的体验
   - 不会出售给第三方
   - 匿名洞察不包含个人身份
```

---

## 三、"仅供参考"免责声明设计

### 3.1 设计原则

| 场景 | 显示方式 | 强度 |
|------|----------|------|
| 日常分析 | 轻量提示 | 弱 |
| 周期报告 | 中等提示 | 中 |
| 心理分析 | 强制确认 | 强 |

### 3.2 UI 设计

#### 3.2.1 日常分析 - 底部小提示
```
┌─────────────────────────────┐
│  💡 此分析仅供参考          │
│  不替代专业心理咨询        │
└─────────────────────────────┘
```

#### 3.2.2 周期报告 - 顶部警告横幅
```
┌─────────────────────────────────────────┐
│ ⚠️ 重要提示                               │
│ 本报告由 AI 自动生成，仅供参考            │
│ 不替代专业心理咨询或医学诊断              │
│ 如有心理困扰，建议咨询专业心理医生        │
│                                         │
│ [我已知悉，继续查看] [了解更多]          │
└─────────────────────────────────────────┘
```

#### 3.2.3 心理深度分析 - 强制确认弹窗
```
┌─────────────────────────────────────────┐
│ 🧠 深度心理分析                          │
│                                         │
│ 您即将查看深度心理分析报告               │
│                                         │
│ ⚠️ 重要提醒：                            │
│ • 本分析基于 AI 算法，仅供参考           │
│ • 不替代专业心理治疗或诊断               │
│ • 如有严重心理困扰，请寻求专业帮助       │
│ • 您的记录将被用于生成此分析            │
│                                         │
│ ☐ 我已阅读并理解上述提示                 │
│                                         │
│ [取消] [确认查看]                        │
└─────────────────────────────────────────┘
```

### 3.3 强制确认逻辑

```typescript
// 心理分析前的强制确认
const beforePsychologicalAnalysis = async () => {
  // 1. 检查是否首次授权
  const hasConsent = localStorage.getItem('psychological_analysis_consent');

  if (!hasConsent) {
    // 2. 显示详细说明
    const confirmed = await showConsentModal({
      title: '深度心理分析',
      content: CONSENT_CONTENT,
      requiresCheck: true,  // 必须勾选
    });

    if (!confirmed) return false;

    // 3. 记录授权
    localStorage.setItem('psychological_analysis_consent', Date.now().toString());
  }

  // 4. 每次显示免责声明
  showDisclaimer('psychological');

  return true;
};
```

### 3.4 内容标注

```markdown
# AI 分析结果标注

---

## 📋 分析结果

[AI 生成的分析内容]

---

### ⚠️ 免责声明

本分析由 AI 自动生成，仅供参考。
- 不替代专业心理咨询或医学诊断
- 结论基于您提供的内容，可能不完整
- 如有心理困扰，建议咨询专业心理医生

**如有紧急情况，请联系：**
- 全国心理援助热线：400-161-9995
- 生命热线：400-821-1215

---
*生成时间：2024-01-15 | 版本：v1.0*
```

---

## 四、提醒机制设计方案

### 4.1 提醒类型

| 类型 | 描述 | 触发条件 | 优先级 |
|------|------|----------|--------|
| **定时提醒** | 固定时间的例行提醒 | 用户设定时间 | P0 |
| **智能提醒** | 基于用户习惯的提醒 | AI 判断最佳时机 | P1 |
| **事件提醒** | 特定事件触发 | 行为触发 | P2 |
| **温情提醒** | 关怀性质的提醒 | 长时间未记录 | P2 |

### 4.2 提醒策略

```typescript
interface ReminderSettings {
  // 基础设置
  enabled: boolean;
  quietHours: {
    start: string;  // "22:00"
    end: string;    // "08:00"
  };

  // 定时提醒
  scheduled: {
    enabled: boolean;
    time: string;       // "09:00"
    days: number[];     // [1,2,3,4,5] 工作日
    message: string;
  };

  // 智能提醒
  intelligent: {
    enabled: boolean;
    maxPerDay: number;  // 每日最多提醒次数
    sensitivity: 'low' | 'medium' | 'high';
  };

  // 温情提醒
  caring: {
    enabled: boolean;
    noRecordDays: number;  // 多少天未记录后提醒
    message: string;
  };
}
```

### 4.3 提醒内容示例

#### 4.3.1 定时提醒
```
🌤️ 早安，Mirror AI 提醒您

新的一天开始了～
今天有什么想法或感受想记录吗？

[立即记录] [稍后再说]
```

#### 4.3.2 智能提醒
```
💡 灵感时刻

根据您的记录习惯，现在是反思今天的好时机～
今天工作中有什么收获吗？

[记录一下] [现在很忙]
```

#### 4.3.3 温情提醒
```
💚 我们在想您

已经 3 天没有看到您的记录了～
无论今天发生了什么，这里都是您可以倾诉的地方。

[我在] [暂时不需要]
```

#### 4.3.4 事件提醒
```
🌙 晚间复盘

睡前 10 分钟的复盘，帮助更好入眠～
今天有什么值得记录的吗？

[记录] [明天再说]
```

### 4.4 提醒渠道

| 渠道 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **推送通知** | 即时、强制 | 需要授权 | ⭐⭐⭐⭐ |
| **邮件** | 正式、可存档 | 打开率低 | ⭐⭐ |
| **微信/飞书** | 方便 | 需要绑定 | ⭐⭐⭐ |
| **日历** | 计划性强 | 手动添加 | ⭐⭐ |

### 4.5 提醒频率设置

```typescript
const REMINDER_FREQUENCY = {
  // 初始用户（培养习惯）
  newUser: {
    morning: '09:00',
    afternoon: '14:00',
    evening: '21:00',
  },

  // 稳定用户（维持习惯）
  stableUser: {
    morning: '09:00',
    evening: '21:00',
  },

  // 低频用户（不打扰）
  lowFrequencyUser: {
    morning: '09:00',
    weekend: '10:00',
  },
};
```

### 4.6 提醒开关 UI

```
┌─────────────────────────────────────┐
│ 🔔 提醒设置                         │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 开启提醒              [开启]    │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 提醒时间                            │
│ ┌─────────────────────────────────┐ │
│ │ 🌅 早晨提醒          09:00    │ │
│ │ 🌙 晚间提醒          21:00    │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 勿扰模式                            │
│ ┌─────────────────────────────────┐ │
│ │ 22:00 - 08:00                  │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 智能提醒                            │
│ ┌─────────────────────────────────┐ │
│ │ 根据习惯智能提醒        [开启]  │ │
│ └─────────────────────────────────┘ │
│                                     │
│ [保存设置]                           │
└─────────────────────────────────────┘
```

---

## 五、总结

| 问题 | 解决方案 | 优先级 |
|------|----------|--------|
| 心理分析提示词 | 分层提示词架构（L1/L2/L3）+ 多模型组合 | P0 |
| 隐私设计 | 数据分级 + 用户主权 + 端到端加密 | P0 |
| 免责声明 | 分场景强制确认 + 底部标注 + 顶部横幅 | P0 |
| 提醒机制 | 多类型提醒 + 智能频率 + 用户可控 | P1 |

---

*文档更新时间：2026-02-14*
